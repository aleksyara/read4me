import React from 'react';
// import { Card, Icon, Image, Feed } from 'semantic-ui-react'
import { Link } from 'react-router-dom';

function StoryCard() { 

    return (
        <h1>Paste your story here</h1>
      );
    }
    
    export default StoryCard;